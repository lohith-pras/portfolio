---
phase: 14-spline-scene-integration
plan: "01"
type: execute
wave: 1
depends_on: ["13-01"]
files_modified:
  - src/components/SplineAbout.tsx
  - src/components/AboutIllustration.tsx
autonomous: false
requirements: ["ABOUT-V2-01"]
user_setup:
  - "Publish Spline scene and provide the scene.splinecode URL"
  - "Provide the exact object name and event type (mouseDown/mouseUp/etc.) that triggers the greeting animation in the Spline scene"

must_haves:
  truths:
    - "On desktop, the Spline character is visible in the About section and plays its greeting animation exactly once when the section scrolls into view"
    - "Subsequent scrolls do not replay the greeting — the character returns to its idle loop"
    - "Lighthouse mobile score on the homepage remains >= 85"
    - "npm run build completes successfully"
  artifacts:
    - "src/components/SplineAbout.tsx"
  key_links:
    - "src/components/AboutIllustration.tsx passes the real scene URL to SplineAbout"
---

<objective>
Wire the published Spline scene URL into SplineAbout.tsx, implement the GSAP ScrollTrigger greeting animation (fires once via `once: true`), handle the race condition where the scene loads after the viewport has already scrolled, and verify Lighthouse mobile score remains >= 85.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
@.agent/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-spline-scene-integration/14-RESEARCH.md
@.planning/phases/14-spline-scene-integration/14-CONTEXT.md
@src/components/SplineAbout.tsx
@src/components/AboutIllustration.tsx
@.planning/phases/13-spline-about-shell/13-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Phase 13 complete — SplineAbout shell with placeholder scene, conditional render, and StaticIllustrationFallback are all live.</what-built>
  <how-to-verify>
    Before execution can begin, the following user inputs are required:

    1. **Published Spline scene URL** — the `.splinecode` URL from spline.design (format: `https://prod.spline.design/XXXX/scene.splinecode`)
    2. **Greeting animation trigger** — the exact object name in the Spline scene AND the event type that triggers the greeting animation:
       - Example A: `spline.emitEvent('mouseDown', 'Character')` — if animation is event-bound
       - Example B: `spline.setVariable('play_greeting', true)` — if animation is variable-driven
    3. **Greeting animation duration** — approximate duration in milliseconds (so the executor can set appropriate timing if needed)

    Please provide all three.
  </how-to-verify>
  <resume-signal>Scene URL: [URL], Event: [object name + event type], Duration: [Xms]</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Wire Real Scene URL into AboutIllustration</name>
  <files>
    - src/components/AboutIllustration.tsx
  </files>
  <read_first>
    - src/components/AboutIllustration.tsx
    - src/components/SplineAbout.tsx
  </read_first>
  <action>
    Replace the placeholder scene URL in AboutIllustration.tsx (or SplineAbout.tsx, wherever it is defined) with the real published scene URL provided by the user in the checkpoint above.

    Define the URL as a module-level constant:
    ```tsx
    const SCENE_URL = 'https://prod.spline.design/USER_SCENE_ID/scene.splinecode'
    ```
  </action>
  <verify>
    URL constant is updated. No reference to the placeholder prod.spline.design/6Wq1Q7YGyM URL remains.
  </verify>
  <acceptance_criteria>
    - Real scene URL is wired.
    - Placeholder URL is removed.
  </acceptance_criteria>
  <done>Real Spline scene URL wired.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GSAP ScrollTrigger Greeting Animation</name>
  <files>
    - src/components/SplineAbout.tsx
  </files>
  <read_first>
    - src/components/SplineAbout.tsx
    - .planning/phases/14-spline-scene-integration/14-RESEARCH.md
    - .planning/phases/14-spline-scene-integration/14-CONTEXT.md
  </read_first>
  <action>
    Modify SplineAbout.tsx to:

    1. Import useGSAP from '@gsap/react' and ScrollTrigger from 'gsap/ScrollTrigger'
    2. Register ScrollTrigger at module top-level: gsap.registerPlugin(ScrollTrigger)
    3. Create refs: containerRef (HTMLDivElement), hasGreeted (boolean)
    4. Implement triggerGreeting(spline) helper function using the exact event call from user's checkpoint response
    5. In useGSAP callback (scope: containerRef), create ScrollTrigger.create({ trigger: containerRef.current, start: 'top 80%', once: true, onEnter: () => { if (!hasGreeted.current && splineRef.current) { hasGreeted.current = true; triggerGreeting(splineRef.current) } } })
    6. In the onLoad callback, add race condition guard: check if section is already in view and fire greeting immediately if so
    7. Attach containerRef to the outer div of SplineAbout

    Follow CLAUDE.md Integration Patterns §1 strictly:
    - Register plugins ONCE at module top-level (not inside useGSAP)
    - Use scope: containerRef in useGSAP
    - NEVER use raw useEffect + gsap.context()

    The greeting fires exactly once. The idle loop is managed by the Spline scene itself.
  </action>
  <verify>
    TypeScript compiles without errors. useGSAP dependency array is correct.
  </verify>
  <acceptance_criteria>
    - ScrollTrigger registered at module top-level.
    - useGSAP with scope: containerRef used correctly.
    - once: true set on ScrollTrigger.create.
    - Race condition guard present in onLoad callback.
    - triggerGreeting uses the scene-specific event call from the user checkpoint.
  </acceptance_criteria>
  <done>GSAP ScrollTrigger greeting animation implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Build Verification</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run 'npm run type-check' then 'npm run build'.
    Both must pass with zero errors.
  </action>
  <verify>
    Check terminal output. Build must complete with ✓ Generating static pages.
  </verify>
  <acceptance_criteria>
    - npm run type-check exits with 0 errors.
    - npm run build generates all static pages successfully.
  </acceptance_criteria>
  <done>Production build passes.</done>
</task>

<task type="auto">
  <name>Task 4: Lighthouse Mobile Audit</name>
  <files></files>
  <read_first>
    - .planning/phases/14-spline-scene-integration/14-RESEARCH.md
  </read_first>
  <action>
    Run Lighthouse mobile audit on the homepage to verify score >= 85.

    Method A (CLI):
    ```bash
    npm run build && npm run start &
    sleep 3
    npx lighthouse http://localhost:3000/en --emulated-form-factor=mobile --output=json --chrome-flags="--headless" 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print('Perf:', d['categories']['performance']['score']*100)"
    ```

    Method B (manual): Open Chrome DevTools → Lighthouse → Mobile → Generate Report on http://localhost:3000/en

    If score >= 85: Record the score in the SUMMARY.md and continue.
    If score < 85: STOP. Report the score and the specific failing audit(s) to the user. Offer options: optimize the scene (reduce polygon count in Spline), defer scene load until after first user interaction, or accept the regression. Do NOT mark the phase complete until the user approves.
  </action>
  <verify>
    Lighthouse performance score on mobile is >= 85.
  </verify>
  <acceptance_criteria>
    - Lighthouse mobile performance score >= 85 confirmed.
  </acceptance_criteria>
  <done>Lighthouse mobile audit passed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Spline 3D character live in About section with GSAP ScrollTrigger greeting animation (once: true) and Lighthouse mobile >= 85 verification.</what-built>
  <how-to-verify>
    1. Run 'npm run dev'
    2. Visit http://localhost:3000/en on desktop (≥768px)
    3. Scroll the About section into view — the Spline character should play its greeting animation exactly once
    4. Scroll back up and then re-scroll — the greeting should NOT replay; the character should continue its idle loop
    5. Resize to mobile (<768px) — the static SVG illustration should appear; DevTools Network tab shows no Spline runtime request
    6. Lighthouse mobile score on http://localhost:3000/en is >= 85
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Real Spline scene URL wired (no placeholder).
- [ ] ScrollTrigger greeting fires exactly once (once: true confirmed).
- [ ] hasGreeted ref prevents double-fire edge cases.
- [ ] npm run build passes.
- [ ] Lighthouse mobile score >= 85.
</verification>

<success_criteria>
- Spline character visible on desktop About section.
- Greeting animation fires once on scroll-in; idle loop runs after.
- Mobile shows StaticIllustrationFallback — no Spline runtime loaded.
- Lighthouse mobile score >= 85.
- npm run build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/14-spline-scene-integration/14-01-SUMMARY.md`
</output>
