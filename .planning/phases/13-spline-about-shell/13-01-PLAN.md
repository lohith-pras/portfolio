---
phase: 13-spline-about-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/hooks/useIsDesktop.ts
  - src/components/SplineAbout.tsx
  - src/components/StaticIllustrationFallback.tsx
  - src/components/AboutIllustration.tsx
  - src/components/AboutSection.tsx
autonomous: true
requirements: ["ABOUT-V2-02", "ABOUT-V2-03"]
user_setup: []

must_haves:
  truths:
    - "On viewport >=768px, AboutSection renders SplineAbout (with placeholder scene URL) — NOT StaticIllustrationFallback"
    - "On viewport <768px, AboutSection renders StaticIllustrationFallback — no Spline JavaScript executes"
    - "npm run build completes successfully — all routes compile, no TypeScript errors"
  artifacts:
    - "src/components/SplineAbout.tsx"
    - "src/components/StaticIllustrationFallback.tsx"
    - "src/components/AboutIllustration.tsx"
    - "src/hooks/useIsDesktop.ts"
  key_links:
    - "src/app/[locale]/page.tsx renders AboutSection which includes AboutIllustration"
---

<objective>
Install @splinetool/react-spline and @splinetool/runtime, create the useIsDesktop hook, SplineAbout.tsx, StaticIllustrationFallback.tsx, and AboutIllustration.tsx components, update AboutSection.tsx to include the illustration slot, and verify that mobile viewports never load the Spline runtime.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
@.agent/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-spline-about-shell/13-RESEARCH.md
@.planning/phases/13-spline-about-shell/13-CONTEXT.md
@src/components/AboutSection.tsx
@src/app/[locale]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Spline Packages</name>
  <files>
    - package.json
  </files>
  <read_first>
    - package.json
  </read_first>
  <action>
    Run: npm install @splinetool/react-spline @splinetool/runtime

    Then verify both packages appear in package.json dependencies.
  </action>
  <verify>
    grep "@splinetool" package.json should show both react-spline and runtime.
  </verify>
  <acceptance_criteria>
    - package.json contains "@splinetool/react-spline" and "@splinetool/runtime".
  </acceptance_criteria>
  <done>Spline packages installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create useIsDesktop Hook</name>
  <files>
    - src/hooks/useIsDesktop.ts
  </files>
  <read_first>
    - .planning/phases/13-spline-about-shell/13-RESEARCH.md
  </read_first>
  <action>
    Create src/hooks/useIsDesktop.ts — a 'use client' hook that uses window.matchMedia('(min-width: 768px)') with a MediaQueryList event listener. Returns false on SSR (initial state) and updates reactively on resize. Must clean up the event listener on unmount.

    Implementation pattern from RESEARCH.md:
    ```ts
    'use client'
    import { useState, useEffect } from 'react'

    export function useIsDesktop(breakpoint = 768): boolean {
      const [isDesktop, setIsDesktop] = useState(false)
      useEffect(() => {
        const mql = window.matchMedia(`(min-width: ${breakpoint}px)`)
        setIsDesktop(mql.matches)
        const handler = (e: MediaQueryListEvent) => setIsDesktop(e.matches)
        mql.addEventListener('change', handler)
        return () => mql.removeEventListener('change', handler)
      }, [breakpoint])
      return isDesktop
    }
    ```
  </action>
  <verify>
    File exists at src/hooks/useIsDesktop.ts and TypeScript compiles it without errors.
  </verify>
  <acceptance_criteria>
    - src/hooks/useIsDesktop.ts exports useIsDesktop function.
    - Hook initialises to false (SSR-safe).
    - Hook listens to MediaQueryList 'change' event and cleans up on unmount.
  </acceptance_criteria>
  <done>useIsDesktop hook created.</done>
</task>

<task type="auto">
  <name>Task 3: Create StaticIllustrationFallback Component</name>
  <files>
    - src/components/StaticIllustrationFallback.tsx
  </files>
  <read_first>
    - .planning/phases/13-spline-about-shell/13-RESEARCH.md
    - src/components/AboutSection.tsx
  </read_first>
  <action>
    Create src/components/StaticIllustrationFallback.tsx — a client component rendering a premium inline SVG.

    Visual requirements (from CONTEXT.md D-07):
    - Warm palette: electric orange (#FF4500) → deep crimson (#C0001A) radial gradient aura
    - Dark background (#101010 or transparent inside the existing container)
    - Geometric/low-poly stylized human silhouette — abstract but clearly humanoid
    - Slight glow effect to suggest warmth and 3D-like presence
    - Fills the aspect-square container of the About section illustration slot (w-full h-full)
    - Must NOT import or reference any @splinetool packages

    The SVG should feel premium — NOT a simple grey placeholder square. This is what mobile users will see permanently.
  </action>
  <verify>
    Component renders without errors. No splinetool imports present.
  </verify>
  <acceptance_criteria>
    - StaticIllustrationFallback.tsx exports the component.
    - SVG uses orange/crimson gradient palette.
    - No @splinetool imports.
  </acceptance_criteria>
  <done>StaticIllustrationFallback created with premium SVG design.</done>
</task>

<task type="auto">
  <name>Task 4: Create SplineAbout Component</name>
  <files>
    - src/components/SplineAbout.tsx
  </files>
  <read_first>
    - .planning/phases/13-spline-about-shell/13-RESEARCH.md
    - .planning/phases/13-spline-about-shell/13-CONTEXT.md
  </read_first>
  <action>
    Create src/components/SplineAbout.tsx — a 'use client' component.

    Requirements:
    - Import from '@splinetool/react-spline/next' (the Next.js-compatible subpath, verified to exist)
    - Accept optional props: sceneUrl (string, defaults to placeholder), onLoad callback, className
    - Wrap the Spline viewer in a React Suspense boundary with a skeleton fallback (div with animate-pulse bg-white/5 rounded-2xl)
    - Store the spline app instance in a ref via onLoad — Phase 14 will use this ref for ScrollTrigger animation
    - Apply sizing: w-full h-full rounded-2xl overflow-hidden
    - The placeholder scene URL for Phase 13 testing: 'https://prod.spline.design/6Wq1Q7YGyM-iab9i/scene.splinecode'

    Interface:
    ```tsx
    interface SplineAboutProps {
      sceneUrl?: string
      onLoad?: (spline: unknown) => void
      className?: string
    }
    ```
  </action>
  <verify>
    TypeScript compiles SplineAbout.tsx without errors. No SSR-unsafe imports at module level.
  </verify>
  <acceptance_criteria>
    - SplineAbout.tsx is a 'use client' component.
    - Uses '@splinetool/react-spline/next' import.
    - Has Suspense boundary with skeleton fallback.
    - Accepts sceneUrl, onLoad, className props.
  </acceptance_criteria>
  <done>SplineAbout component created with placeholder scene URL and onLoad ref capture.</done>
</task>

<task type="auto">
  <name>Task 5: Create AboutIllustration Client Wrapper</name>
  <files>
    - src/components/AboutIllustration.tsx
  </files>
  <read_first>
    - src/components/AboutSection.tsx
    - src/components/SplineAbout.tsx
    - src/components/StaticIllustrationFallback.tsx
    - src/hooks/useIsDesktop.ts
    - .planning/phases/13-spline-about-shell/13-CONTEXT.md
  </read_first>
  <action>
    Create src/components/AboutIllustration.tsx — a 'use client' component that:
    1. Calls useIsDesktop() hook
    2. Returns <StaticIllustrationFallback /> when !isDesktop
    3. Returns <SplineAbout sceneUrl={PLACEHOLDER_URL} /> when isDesktop
    4. Applies the same container styling as the current About section illustration slot:
       aspect-square w-full max-w-[400px] mx-auto bg-white/5 rounded-2xl border border-white/10 relative overflow-hidden

    This component is intentionally thin — it only owns the conditional logic. Both child components handle their own internal layout.

    Important: Do NOT render SplineAbout server-side at all. The useIsDesktop hook's initial false state ensures the fallback renders first, then the client updates to SplineAbout after hydration on desktop.
  </action>
  <verify>
    Component compiles cleanly. Uses useIsDesktop hook.
  </verify>
  <acceptance_criteria>
    - AboutIllustration.tsx exports the component.
    - Uses useIsDesktop hook to conditionally render SplineAbout or StaticIllustrationFallback.
    - Applies container styling with aspect-square, rounded-2xl, border.
  </acceptance_criteria>
  <done>AboutIllustration client wrapper created.</done>
</task>

<task type="auto">
  <name>Task 6: Update AboutSection to Use AboutIllustration</name>
  <files>
    - src/components/AboutSection.tsx
  </files>
  <read_first>
    - src/components/AboutSection.tsx
    - src/components/AboutIllustration.tsx
  </read_first>
  <action>
    Modify AboutSection.tsx to replace the current static illustration div (with the SVG placeholder) with <AboutIllustration />.

    The current illustration slot (lines 19–25) is:
    ```tsx
    <div className="order-1 md:order-2 aspect-square w-full max-w-[400px] mx-auto bg-white/5 rounded-2xl border border-white/10 flex items-center justify-center relative overflow-hidden">
      {/* Static illustration placeholder for v1 */}
      <div className="absolute inset-0 bg-gradient-to-tr from-accent/20 to-transparent" />
      <svg ...>...</svg>
    </div>
    ```

    Replace this entire div with:
    ```tsx
    <AboutIllustration />
    ```

    Import AboutIllustration at the top of the file. AboutSection.tsx itself does NOT need 'use client' — it remains a server component since AboutIllustration handles client-side logic internally.
  </action>
  <verify>
    AboutSection.tsx compiles cleanly. The illustration slot is replaced with <AboutIllustration />.
  </verify>
  <acceptance_criteria>
    - AboutSection.tsx imports and renders <AboutIllustration />.
    - The old static SVG div is removed.
    - AboutSection.tsx does NOT have 'use client' directive.
  </acceptance_criteria>
  <done>AboutSection updated to use AboutIllustration client wrapper.</done>
</task>

<task type="auto">
  <name>Task 7: Build Verification</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run 'npm run type-check' then 'npm run build'.
    Both must pass with zero errors.
  </action>
  <verify>
    Check terminal output. Build must complete with ✓ Generating static pages and all route slugs visible.
  </verify>
  <acceptance_criteria>
    - npm run type-check exits with 0 errors.
    - npm run build generates all static pages (20/20 or equivalent).
  </acceptance_criteria>
  <done>Production build verified successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SplineAbout component shell with useIsDesktop conditional render — desktop shows Spline placeholder scene, mobile shows premium SVG fallback. No Spline JavaScript on mobile.</what-built>
  <how-to-verify>
    1. Run 'npm run dev'
    2. Visit http://localhost:3000/en at desktop viewport (≥768px) — About section right column should show the Spline 3D scene (placeholder scene, possibly a generic Spline object)
    3. Resize to mobile width (<768px) — About section should show the SVG illustration (orange/crimson geometric figure)
    4. In DevTools Network tab at mobile width — confirm NO request to prod.spline.design or @splinetool/runtime
    5. The SVG fallback should look premium — warm gradient aura, stylized figure, NOT a grey box
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run 'npm run type-check' successfully.
- [ ] Run 'npm run build' successfully.
- [ ] Confirm no @splinetool imports in StaticIllustrationFallback.tsx.
- [ ] Confirm AboutSection.tsx has no 'use client' directive.
</verification>

<success_criteria>
- @splinetool/react-spline and @splinetool/runtime installed.
- useIsDesktop hook created in src/hooks/useIsDesktop.ts.
- SplineAbout.tsx created, imports from @splinetool/react-spline/next, uses Suspense.
- StaticIllustrationFallback.tsx created with premium SVG design (orange/crimson palette).
- AboutIllustration.tsx created — conditionally renders the two components.
- AboutSection.tsx updated to use AboutIllustration (no 'use client').
- Production build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-spline-about-shell/13-01-SUMMARY.md`
</output>
